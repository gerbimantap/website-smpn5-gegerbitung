import { supabase, configured } from "./supabase.js";
import { SUPABASE_URL } from "./config.js";

const el=id=>document.getElementById(id);
const fallback={school_name:"SMPN 5 Gegerbitung",principal_name:"Nengsri Rohimah, Munazah, S.Pd., M.Pd.",vision:"Mewujudkan peserta didik yang berkarakter, berprestasi, mandiri, dan berwawasan.",mission:"Menyelenggarakan pembelajaran yang aktif, inovatif, berkarakter, dan berorientasi pada prestasi.",principal_message:"Selamat datang di website resmi SMPN 5 Gegerbitung. Mari bersama membangun lingkungan pendidikan yang aman, inspiratif, dan berprestasi."};
function imageUrl(path){if(!path)return "./logo%20sekolah.jpeg";if(String(path).startsWith("http"))return path;return `${SUPABASE_URL}/storage/v1/object/public/school-media/${String(path).replace(/^\/+/,"")}`}
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}
function stripHtml(v){return String(v??"").replace(/<[^>]*>/g,"")}
function card(item,type,index=0){const title=item.title||item.name||"Tanpa judul";const text=item.excerpt||item.description||item.content||"Informasi sekolah.";const img=item.featured_image_path||item.image_path;const tag=item.category||(type==="achievement"?"Program Unggulan":"Kegiatan");const news=type==="news";return `<article class="card${news?" card-clickable":""}" ${news?`data-news-index="${index}" tabindex="0" role="button"`:""}>${img?`<img class="card-img" src="${imageUrl(img)}" alt="${esc(title)}" onerror="this.src='./logo%20sekolah.jpeg'">`:""}<div class="card-body"><span class="tag">${esc(tag)}</span><h3>${esc(title)}</h3><p>${stripHtml(text).slice(0,180)}${stripHtml(text).length>180?"…":""}</p>${news?`<button type="button" class="btn btn-primary news-read-button" data-news-index="${index}">Baca selengkapnya</button>`:""}</div></article>`}
function teacherCard(t){const name=esc(t.name||"Nama belum diisi"),role=esc(t.position||"Guru/Tendik"),subject=esc(t.subject||""),photo=imageUrl(t.photo_path),id=esc(t.id);return `<article class="teacher-card"><img class="teacher-photo" src="${photo}" alt="Foto ${name}" onerror="this.src='./logo%20sekolah.jpeg'"><div class="teacher-body"><span class="teacher-role">${role}</span><h3>${name}</h3><p class="teacher-meta">${subject?`<b>Mata Pelajaran:</b> ${subject}`:"Tenaga pendidik dan kependidikan SMPN 5 Gegerbitung."}</p><div class="teacher-detail"><button class="btn btn-soft" data-teacher-id="${id}">Lihat Profil Lengkap</button></div></div></article>`}
function showTeacher(t){const body=el("teacherModalBody");if(!body)return;body.innerHTML=`<div class="teacher-profile"><img src="${imageUrl(t.photo_path)}" alt="Foto ${esc(t.name||"Guru/Tendik")}"><div><span class="teacher-role">${esc(t.position||"Guru/Tendik")}</span><h2>${esc(t.name||"Nama belum diisi")}</h2><div class="teacher-info"><div><b>NIP</b>${esc(t.nip||"-")}</div><div><b>NUPTK</b>${esc(t.nuptk||"-")}</div><div><b>Jenis Kelamin</b>${t.gender==="L"?"Laki-laki":t.gender==="P"?"Perempuan":"-"}</div><div><b>Mata Pelajaran</b>${esc(t.subject||"-")}</div><div><b>Pendidikan</b>${esc(t.education||"-")}</div></div></div></div>`;el("teacherModal").classList.add("show");el("teacherModal").setAttribute("aria-hidden","false")}
function closeTeacher(){const m=el("teacherModal");if(m){m.classList.remove("show");m.setAttribute("aria-hidden","true")}}
function ensureNewsModal(){const modal=el("newsModal");if(modal){bindNewsModal(modal);return}const style=document.createElement("style");style.textContent=`#newsModal{position:fixed;inset:0;background:rgba(15,23,42,.72);display:none;place-items:center;padding:20px;z-index:9999;pointer-events:auto}#newsModal.show{display:grid}#newsModal .news-modal-card{width:min(820px,100%);max-height:90vh;overflow:auto;background:#fff;border-radius:24px;position:relative;padding:30px;box-shadow:0 25px 70px rgba(0,0,0,.25);z-index:10000}#newsModal .news-modal-img{width:100%;max-height:380px;object-fit:cover;border-radius:18px;margin-bottom:20px}#newsModal .news-modal-title{font-size:32px;line-height:1.25;margin:15px 0 8px}#newsModal .news-modal-date{font-size:13px;color:#64748b;margin-bottom:20px}#newsModal .news-modal-content{font-size:15px;line-height:1.9;color:#334155}@media(max-width:560px){#newsModal{padding:10px}#newsModal .news-modal-card{padding:20px}#newsModal .news-modal-title{font-size:25px}}`;document.head.appendChild(style);const newModal=document.createElement("div");newModal.id="newsModal";newModal.innerHTML=`<div class="news-modal-card"><div id="newsModalBody"></div></div>`;document.body.appendChild(newModal);bindNewsModal(newModal)}
function bindNewsModal(modal){if(!modal.dataset.bound){modal.dataset.bound="1";modal.addEventListener("click",e=>{if(!e.target.closest(".news-modal-card")){closeNews()}})}}
function showNews(item){ensureNewsModal();const body=el("newsModalBody");if(!body||!item)return;const title=esc(item.title||"Tanpa judul"),img=item.featured_image_path||item.image_path,date=item.published_at?new Date(item.published_at).toLocaleDateString("id-ID",{day:"numeric",month:"long",year:"numeric"}):"",category=esc(item.category||"Berita Sekolah"),content=stripHtml(item.content||item.excerpt||item.description||"Informasi sekolah.").replace(/\n/g,"<br>");body.innerHTML=`${img?`<img class="news-modal-img" src="${imageUrl(img)}" alt="${title}">`:""}<span class="tag">${category}</span><h2 class="news-modal-title">${title}</h2>${date?`<div class="news-modal-date">${date}</div>`:""}<div class="news-modal-content">${content}</div>`;el("newsModal").classList.add("show");el("newsModal").setAttribute("aria-hidden","false");document.body.style.overflow="hidden"}
function closeNews(){const m=el("newsModal");if(m){m.classList.remove("show");m.setAttribute("aria-hidden","true");document.body.style.overflow=""}}
function ensureGalleryModal(){const modal=el("galleryModal");if(modal){bindGalleryModal(modal);return}const style=document.createElement("style");style.textContent=`#galleryModal{position:fixed;inset:0;background:rgba(15,23,42,.72);display:none;place-items:center;padding:20px;z-index:9998}#galleryModal.show{display:grid}#galleryModal .gallery-modal-card{width:min(900px,100%);max-height:90vh;overflow:auto;background:#fff;border-radius:24px;padding:30px;box-shadow:0 25px 70px rgba(0,0,0,.25)}#galleryModal .gallery-modal-title{font-size:30px;line-height:1.3;margin:0 0 8px}#galleryModal .gallery-modal-meta{font-size:13px;color:#64748b;margin-bottom:22px}#galleryModal .gallery-modal-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}#galleryModal .gallery-modal-grid img{width:100%;height:220px;object-fit:cover;border-radius:14px;background:#e0f2fe}@media(max-width:650px){#galleryModal{padding:10px}#galleryModal .gallery-modal-card{padding:20px}#galleryModal .gallery-modal-title{font-size:24px}#galleryModal .gallery-modal-grid{grid-template-columns:repeat(2,1fr)}#galleryModal .gallery-modal-grid img{height:170px}}`;document.head.appendChild(style);const newModal=document.createElement("div");newModal.id="galleryModal";newModal.innerHTML=`<div class="gallery-modal-card"><div id="galleryModalBody"></div></div>`;document.body.appendChild(newModal);bindGalleryModal(newModal)}
function bindGalleryModal(modal){if(!modal.dataset.bound){modal.dataset.bound="1";modal.addEventListener("click",e=>{if(!e.target.closest(".gallery-modal-card")){closeGallery()}})}}
function showGallery(album){ensureGalleryModal();const body=el("galleryModalBody");if(!body||!album)return;const title=esc(album.title||"Galeri Sekolah"),date=album.event_date?new Date(album.event_date).toLocaleDateString("id-ID",{day:"numeric",month:"long",year:"numeric"}):"",images=(album.gallery_images||[]).filter(x=>x.image_path);body.innerHTML=`<h2 class="gallery-modal-title">${title}</h2>${date?`<div class="gallery-modal-meta">${date} • ${images.length} foto</div>`:`<div class="gallery-modal-meta">${images.length} foto</div>`}<div class="gallery-modal-grid">${images.map(i=>`<img src="${imageUrl(i.image_path)}" alt="${esc(i.caption||album.title||"Galeri sekolah")}" onerror="this.src='./logo%20sekolah.jpeg'">`).join("")}</div>`;el("galleryModal").classList.add("show");el("galleryModal").setAttribute("aria-hidden","false");document.body.style.overflow="hidden"}
function closeGallery(){const m=el("galleryModal");if(m){m.classList.remove("show");m.setAttribute("aria-hidden","true");document.body.style.overflow=""}}
async function load(){if(!configured){el("newsGrid").innerHTML=`<div class="empty">Supabase belum dikonfigurasi.</div>`;return}const [{data:school},{data:news},{data:cur},{data:stu},{data:ach},{data:teachers,error:teacherError}]=await Promise.all([supabase.from("school_settings").select("*").limit(1).maybeSingle(),supabase.from("news").select("*").eq("status","published").order("published_at",{ascending:false}).limit(6),supabase.from("curriculum_activities").select("*").eq("status","published").order("activity_date",{ascending:false}).limit(6),supabase.from("student_activities").select("*").eq("status","published").order("activity_date",{ascending:false}).limit(6),supabase.from("achievements").select("*").order("year",{ascending:false}).limit(6),supabase.from("teachers").select("*").eq("is_active",true).order("sort_order",{ascending:true}).order("name",{ascending:true})]);const s=school||fallback;el("schoolName").textContent=s.school_name||fallback.school_name;el("principalName").textContent=s.principal_name||fallback.principal_name;el("vision").textContent=s.vision||fallback.vision;el("mission").textContent=s.mission||fallback.mission;el("principalMessage").textContent=s.principal_message||fallback.principal_message;el("statTeachers").textContent=teacherError?"-":(teachers?.length??0);const studentCount=await supabase.from("students").select("id",{count:"exact",head:true});el("statStudents").textContent=studentCount.count??"-";el("statAchievements").textContent=ach?.length??"-";el("statActivities").textContent=((cur||[]).length+(stu||[]).length)||"-";el("teacherGrid").innerHTML=teacherError?`<div class="empty">Kolom foto guru belum siap.</div>`:(teachers?.length?teachers.map(teacherCard).join(""):`<div class="empty">Belum ada data guru & tendik yang aktif.</div>`);if(!teacherError)el("teacherGrid").querySelectorAll("[data-teacher-id]").forEach(btn=>btn.addEventListener("click",()=>showTeacher(teachers.find(t=>String(t.id)===btn.dataset.teacherId))));el("newsGrid").innerHTML=news?.length?news.map((x,i)=>card(x,"news",i)).join(""):`<div class="empty">Belum ada berita.</div>`;if(news?.length){el("newsGrid").querySelectorAll(".news-read-button").forEach(btn=>btn.addEventListener("click",e=>{e.preventDefault();e.stopPropagation();showNews(news[Number(btn.dataset.newsIndex)])}));el("newsGrid").querySelectorAll(".card-clickable").forEach(cardEl=>cardEl.addEventListener("click",e=>{if(e.target.closest(".news-read-button"))return;showNews(news[Number(cardEl.dataset.newsIndex)])}));}const activities=[...(cur||[]),...(stu||[])].slice(0,6);el("activityGrid").innerHTML=activities.length?activities.map(x=>card(x,"activity")).join(""):`<div class="empty">Belum ada kegiatan.</div>`;el("achievementGrid").innerHTML=ach?.length?ach.map(x=>card(x,"achievement")).join(""):`<div class="empty">Belum ada program unggulan.</div>`;const {data:albums}=await supabase.from("gallery_albums").select("*,gallery_images(*)").eq("is_published",true).order("event_date",{ascending:false}).limit(8);el("galleryGrid").innerHTML=albums?.length?albums.map((album,i)=>{const images=(album.gallery_images||[]).filter(x=>x.image_path);const cover=images[0];return `<article class="card gallery-album-card"><div style="position:relative">${cover?`<img class="card-img" src="${imageUrl(cover.image_path)}" alt="${esc(album.title||"Galeri sekolah")}" onerror="this.src='./logo%20sekolah.jpeg'">`:""}</div><div class="card-body"><span class="tag">Galeri</span><h3>${esc(album.title||"Galeri Sekolah")}</h3><p>${images.length} foto${album.event_date?` • ${new Date(album.event_date).toLocaleDateString("id-ID",{day:"numeric",month:"long",year:"numeric"})}`:""}</p><button type="button" class="btn btn-primary gallery-read-button" data-gallery-index="${i}">Lihat selengkapnya</button></div></article>`}).join(""):`<div class="empty">Belum ada foto galeri.</div>`;if(albums?.length){el("galleryGrid").querySelectorAll(".gallery-read-button").forEach(btn=>btn.addEventListener("click",e=>{e.preventDefault();e.stopPropagation();showGallery(albums[Number(btn.dataset.galleryIndex)])}))}}
ensureNewsModal();ensureGalleryModal();el("teacherModalClose")?.addEventListener("click",closeTeacher);el("teacherModal")?.addEventListener("click",e=>{if(e.target.id==="teacherModal")closeTeacher()});document.addEventListener("keydown",e=>{if(e.key==="Escape"){closeTeacher();closeNews();closeGallery()}});load().catch(err=>console.error(err));